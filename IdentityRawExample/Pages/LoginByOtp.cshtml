@page
@model IdentityRawExample.Pages.LoginByOtpModel
@{
}
<div class="row">
    <div class="col-md-6">
        <form id="LoginByOtp-form" method="post" asp-route-returnUrl="@Model.ReturnUrl">
            <div asp-validation-summary="All" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="loginByOtp.PhoneNumber"></label>
                <input asp-for="loginByOtp.PhoneNumber" class="form-control" placeholder="09394022308 مثال"/>
                       
                <span asp-validation-for="loginByOtp.PhoneNumber" class="text-danger"></span>
            </div>
          
            <button type="submit" class="btn btn-primary">درخواست کد</button>
        </form>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
